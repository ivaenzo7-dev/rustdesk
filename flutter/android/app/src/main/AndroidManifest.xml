<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carriez.flutter_hbb"
    android:sharedUserId="io.droiddesk.shared">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- RECORD_AUDIO removed for rental fleet: phone-side mic capture is never
         used by the renter flow, and keeping the permission triggers an extra
         consent dialog on first launch / Android 14 mic indicator on every
         session. Audio paths in AudioRecordHandle/MainService gracefully fail
         when permission is absent (checkSelfPermission -> DENIED). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Samsung Knox RemoteInjection: privileged input so remote taps reach
         secure surfaces (Google Play sign-in, runtime-permission dialogs,
         FLAG_SECURE windows) that reject AccessibilityService gestures.
         Effective only on Samsung with an activated KPE license; harmless
         elsewhere (KnoxInput falls back to dispatchGesture). -->
    <uses-permission android:name="com.samsung.android.knox.permission.KNOX_REMOTE_CONTROL" />

    <queries>
        <intent>
            <!-- https://developer.android.com/training/package-visibility/use-cases#open-urls-custom-tabs -->
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="RustDesk"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <!-- Knox SDK shared library: puts com.samsung.android.knox.* (incl.
             RemoteInjectionManager) on the app classpath at runtime on Samsung.
             required=false -> app still installs/runs on non-Samsung devices
             (classes absent -> KnoxInput falls back to dispatchGesture). -->
        <uses-library android:name="knoxsdk" android:required="false" />
        <uses-library android:name="com.samsung.android.knox.knoxsdk" android:required="false" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!--ACTION_BOOT_COMPLETED for debug test on no root device-->
                <action android:name="com.carriez.flutter_hbb.DEBUG_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".InputService"
            android:enabled="true"
            android:exported="false"
            android:label="RustDesk Input"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode|navigation"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent for deep linking-->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rustdesk" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PermissionRequestTransparentActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Transparent" />

        <service
            android:name=".MainService"
            android:enabled="true"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name=".FloatingWindowService"
            android:enabled="true" />

        <!-- KeepAliveService: держит процесс живым → InputService не убивается -->
        <service
            android:name=".KeepAliveService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!-- RentalBannerService: полноэкранная штора «ИДЁТ АРЕНДА» во время сеанса.
             Скрывает содержимое экрана от сотрудника, держащего телефон;
             в MediaProjection-стрим (для админа) штора не попадает — см.
             RentalBannerService.applySkipScreenshot + MainService.recreateVirtualDisplay. -->
        <service
            android:name=".RentalBannerService"
            android:foregroundServiceType="specialUse"
            android:enabled="true"
            android:exported="false">
            <!-- Требуется при targetSdk >= 34 для FGS type=specialUse.
                 Включаем заранее, чтобы при будущем апгрейде SDK не сломалось. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="rental_privacy_overlay" />
        </service>

        <!--
 Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java
        -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
